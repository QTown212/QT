import matb.pyot as plt
import numas np

#plot 1:
x = npry(0, 3])
y = np.array([1, 1])

plt.bplot(1, 2, 1)
plt.plot(x,y)

#plot 2:
x = np.array([0, 1, 0, 3])
y = np.array([10, 20, 30, 40])

plt.subplot(1, 2, 2)
plt.plot(x,y)

plt.show()
